# アパート管理アプリ — 開発計画（前提を作り直した版）

## Context

祖父（70〜80代）が紙と記憶で管理しているアパート（1棟10部屋前後）の管理アプリを、家族が新規開発する。目的は家賃計算の効率化ではなく、**「なぜその判断をしたか」という経緯ごと、祖母 →（息子・娘）へ引き継げる家族のアーカイブを作ること**。主端末はスマホ。利用者は実質1〜2人。開発者はアプリ開発が専門外。**費用は完全無料が絶対条件**。

### 前提の変更（ここが前版との最大の違い）

**祖父はスマホを普通に使える。**「デジタル機器に不慣れ」という当初の前提を取り下げる。これに伴い、その前提から導いていた次の制約を**すべて撤回**する。

| 撤回するもの | 理由 |
|---|---|
| 初回は「②家賃の入金」だけに絞る | 使いこなせる前提なら、①〜④を最初から作ってよい |
| 開発初日を「聞き取りの日」にする | 実データは作りながら入れれば足りる |
| 振込の「通帳未確認」を第3の状態として持つ | 「済／未」の2状態で十分。過剰設計 |
| 取り消しの表示を8秒に延ばす | 一般的な5秒に戻す |
| 紙のノートを当分続ける前提 | 判断は祖父に委ねる。設計に持ち込まない |

**新たに正式機能として入れるもの**（祖父の要望）:
- **音声入力** — 経緯メモを、話して書けるようにする
- **写真添付** — 修繕箇所・書類・現況を写真で残す

「打ちにくい人のための代替手段」ではなく、**文字より速く・多く残せるから入れる**。この2つは「アーカイブを残す」という本来の目的に、文字入力より直接効く。

これまでに、動く画面見本（`F:\apartment-app\mockup.html`）とデザイン3案（`F:\apartment-app\design\`）を作成済み。

## デザイン確定：A案の構造 ＋ C案の寸法 ＋ B案の言葉づかい

- **構造はA案** — 長いメモが自然に載る構造を持つのはA案だけ。経緯メモが主役である以上ここは動かせない
- **寸法・コントラストはC案** — 判読性の数値をC案の基準に揃える
- **言葉づかいはB案** — 入金は「済／未」、日付は令和表記＋算用数字＋曜日（「令和8年8月10日（月）」）、バックアップは「控えを家族に送る」

### 3案共通で必ず直す実装上の欠陥

- 補足テキストの**最小17px**（現状12〜15pxが混在）。サイズは全て `rem` にし、端末の文字拡大に追随させる
- タップできる場所は**最小64px**。`height` 固定をやめ `min-height` に（現状「もどる」が48px）
- **`:active` と `:focus-visible` を全押下要素に定義**（現行3案は定義ゼロ。`mockup.html` にはあったので退行している）
- SVGアイコンの線幅を **2.5〜2.8** に（2.1は細く、にじんで消える）
- **警告0件のときの表示**を用意（「今日は、急いですることはありません」）。空白は「壊れた」と読まれる
- 画面下部に**「ホームにもどる」を常設**（上端は片手持ちの親指が届かない）
- 「済」は静かに、「未」を強く。注意を向けるべきは未払いのほう

## v1のスコープ（①〜④すべて ＋ 音声 ＋ 写真）

**① 入居者・契約**　一覧（更新が近い順）／詳細／契約のいきさつメモ／更新警告（60日・30日）
**② 家賃の入金**　月ごとの一覧／「済・未」を1タップ切替／入金日の記録／操作履歴／取り消し（5秒）／去年まで遡れる月送り
**③ 修繕・費用**　一覧（修繕／固定費）／詳細／新規登録／**経緯メモの音声入力**／**写真添付**
**④ 空室の状況**　タイル一覧。**契約データから自動で導出する**（独立した入力を持たない＝二重管理をなくす）
**共通**　控えを家族に送る（JSON ＋ 読める1枚）／印刷／最終送信日の表示

**v1では作らないもの**: ログイン、複数棟対応、確定申告用の集計、年間予定表

## データ設計（コードより先に確定させる）

`mockup.html` の `PAYMENTS = { "2026-08": { "101": true } }` は**破棄**。部屋番号をキーにすると入居者が変わった瞬間に過去の記録が読めなくなり、金額も入金日も持てない。

```
rooms       部屋（101〜204）
tenants     人（氏名・かな・電話・保証人・連絡のしかた※）
leases      契約（部屋ID, 人ID, 開始日, 終了日, 敷金, 礼金）
rentTerms   家賃の履歴（契約ID, 適用開始年月, 家賃, 管理費, 変更理由）
payments    入金（契約ID, 対象年月, 金額, 入金日, 方法, メモ）
paymentLog  入金操作の履歴（誰が・いつ・何を・どう変えたか）
expenses    費用（写真ID配列を含む）
photos      写真（Blob。控えJSONとは分けて送る）
notes       いきさつメモ（対象種別, 対象ID, 日付, 記入者, 本文, 音声入力かどうか）
```

※「連絡のしかた」を独立した欄にする（例：耳が遠いので手紙が確実／夜勤で日中は不在）。契約書のどこにも書かれない、頭の中にしかない情報で、**引き継ぎで最初に失われる種類のもの**。

**上書きせず履歴として残す3つ**
- **家賃の変更** — 「2022年の更新で2,000円下げた」は祖父の判断そのもの。上書きすると消える
- **契約の更新** — 更新のたびに行を足す
- **退去と再入居** — `status` を書き換えず、契約を終了して新しい契約を作る

**型の約束**
- 日付は必ず `'2026-08-25'` の**文字列**。`new Date("2026-08-25")` はUTC解釈で日本時間だと1日ずれる。`mockup.html` の `parseDate()`（`new Date(y, m-1, d)` と分解）が正しいので `src/lib/date.ts` にそのまま移す
- 金額は**整数の円のみ**（小数厳禁）
- 全レコードに `id`（`crypto.randomUUID()`）/ `createdAt` / `updatedAt`
- 削除は**論理削除**（`deletedAt`）
- 「今日」は毎回**時刻を切り落として**使う（境界が±1日ずれる）

## 音声入力の作り方

**2段構えにする。**

1. **OS標準のキーボード音声入力** — テキスト欄を大きく取っておけば、キーボードのマイクボタンがそのまま使える。実装ゼロで、最も確実
2. **「🎤 押して話す」ボタン** — `webkitSpeechRecognition`（`lang='ja-JP'`, `continuous:true`, `interimResults:true`）。話している途中の文字が薄く出て、確定すると本文に入る

**必ず守ること**
- 認識結果は**必ず編集できるテキストとして欄に入れる**。自動で保存・確定しない
- 認識には**ネット接続が必要**。オフライン時はボタンを無効化し、「文字で書く」に誘導する
- iOS Safariは1回の認識が1分ほどで自動終了する → `onend` で自動的に再開する
- 非対応ブラウザでは**ボタン自体を出さない**（押せないボタンは故障に見える）
- マイク許可は**初回に理由を説明してから**求める（いきなりのOSダイアログは拒否されやすい）

## 写真添付の作り方

- `<input type="file" accept="image/*" capture="environment">` でカメラを直接起動。複数選択も可
- 保存前に **`createImageBitmap(file, { imageOrientation:'from-image' })`** で向きを正し、canvasで**長辺1600px・JPEG品質0.8**に圧縮 → 1枚200〜400KB
  - iPhoneのHEICもcanvas経由で確実にJPEGになる。EXIFの回転情報もここで解消される
- Dexieに **Blob** で保存。表示は `URL.createObjectURL`、画面を離れるとき `revokeObjectURL`（解放しないとメモリを食い続ける）
- **控えJSONには写真を入れない**（base64にすると1.33倍に膨らみLINEで送れなくなる）。`navigator.share({ files: [...] })` でJSONと写真をまとめて渡す

## 技術構成

| 層 | 採用 | 理由 |
|---|---|---|
| React 19 + TypeScript + Vite 6 | 情報が多く、将来他人に引き継ぎやすい |
| **素のCSS ＋ tokens.css ＋ CSS Modules**（Tailwind不採用） | v4は情報が少なくハマりやすい。`mockup.html` は素のCSSで十分書けている |
| Dexie（IndexedDB） | 端末内保存。`useLiveQuery` で画面が自動更新され、状態管理ライブラリが不要になる |
| vite-plugin-pwa（`registerType:'autoUpdate'`） | 「更新しますか？」は無視されるので黙って更新する |
| Vitest | **バックアップの往復テスト**と**マイグレーションテスト**を最優先 |
| GitHub Actions → GitHub Pages（**公開リポジトリ**） | 無料。※下記の訂正を参照 |

**費用は月0円**（ホスティング・CI・ストア登録すべて不要。データは端末内）。

### 訂正：リポジトリは「公開」にする

以前の版で「プライベートリポジトリのまま無料でPages公開できる」と書いたのは**誤り**。
GitHub Pages をプライベートリポジトリから公開するには **GitHub Pro（月$4）が必要**で、無料プランでは公開リポジトリのみ。無料を維持するため**公開リポジトリ**を採用する。

前提として、**リポジトリに入るのはプログラムだけで、入居者の氏名・家賃・電話番号は一切入らない**（それらは端末のIndexedDBにのみ存在する）。そのうえで次を守る。

- 見本データは**架空の名前のまま**にし、実在の入居者名をコミットしない
- 控えJSON（`*.backup.json`、`控え*.json`、`data/`）を `.gitignore` で送信対象から除外済み
- 実データを扱うのは、あくまで祖父のスマホの中だけ

**先に手を打つ箇所**
- `vite.config.ts` に `base: '/リポジトリ名/'` を書かないと真っ白になる
- ルーティングは **HashRouter**（BrowserRouterは直接URLで404）
- File System Access API は **iOS Safariで動かない** → `navigator.share` とダウンロードのフォールバック前提
- 書き出すJSONに **`schemaVersion` を必ず入れる**
- **入力する端末は1台**と決める（2台で入力した矛盾は解決が難しい）。他は閲覧専用
- iOSは**ホーム画面のアイコンを消すとデータも消える** → 控えの共有を早い段階で作る

## 進め方（実働5〜7週間）

| フェーズ | 期間 | 内容 |
|---|---|---|
| **0. 土台** | 1〜2日 | Vite+React+TS、プライベートリポジトリ、Actions→Pages。**空のアプリを祖父のスマホのホーム画面から起動できる**ところまで |
| **1. データ層とバックアップ** | 3〜4日 | Dexieスキーマ、`lib/backup.ts`（書き出し・読み込み・schemaVersion）、Vitestで往復テスト。**データを守る仕組みをデータより先に作る** |
| **2. ②家賃の入金** | 5日 | 一番よく使う画面から。`renewalLevel` `unpaidRooms` `jpDate` `yen`（`mockup.html`で検証済み）を `src/lib/` に移し金額ベースへ |
| **3. ①入居者・契約** | 5日 | 一覧・詳細・更新警告・いきさつメモ |
| **4. ③修繕・費用 ＋ 音声 ＋ 写真** | 6〜7日 | メモが主役の画面なので、音声入力と写真をここで完成させる |
| **5. ④空室の状況** | 2〜3日 | 契約データから導出するだけなので短い |
| **6. 共有・印刷・仕上げ** | 3〜4日 | 控えをLINEで送る、印刷、実機での調整 |

各フェーズの終わりに**必ず公開して祖父のスマホで触ってもらう**。作り切ってから見せない。

## v2以降の優先順位（2026-08-11 にユーザーが方向を修正）

**主は「高齢者が問題なく使える物件管理」。引き継ぎ・アーカイブは従。**

v1の途中まで、控え・印刷・年表といった「家族へ引き継ぐ」側の機能を厚く見ていたが、
ユーザーから「引き継ぎ機能を増やすより、**物件管理そのものを充実させてほしい**」と指示があった。
以後、迷ったら**日々の管理でその場で役に立つか**を先に問うこと。
アーカイブとしての価値（判断の経緯を残す）は目的として生きているが、
**それを機能の数で表現しにいかない**。日々の管理をきちんと作れば経緯は自然に貯まる。

**次にやる（物件管理そのもの）**

1. ✅ **確定申告用の年間集計** — 2026-08-11 実装（`lib/yearly.ts` / `screens/Yearly.tsx`）
2. ✅ **⑤年間の予定** — 2026-08-11 実装（`lib/schedules.ts` / `screens/Schedules.tsx`）。
   火災保険・地震保険の更新／固定資産税の納期／消防設備点検を登録し、近づいたらホームで知らせる
3. 給湯器・エアコンの年式管理 — 交換時期の判断に要る
4. 更新案内を3か月前に通知（いまは60日・30日前）
5. 退去立会いと敷金精算
6. 駐車場と車のナンバー／草刈り・剪定

**あとで（引き継ぎ寄り。急がない）**

緊急連絡先と保証人の1枚印刷／「**この建物のあゆみ（年表）**」— 修繕・更新・家賃改定・入退去を
1本の縦の年表にする。記録が積み上がって見えること自体がアーカイブを表現する。
大家Oneにはない切り口だが、**日々の管理の役には立たない**ので後回しにする。

## 検証方法

- **フェーズ0の完了条件**: 祖父の実機でURLを開き、ホーム画面に追加でき、アイコンから全画面で起動する
- **フェーズ1の完了条件**: Vitestで「書き出す→読み込む→完全一致」。古い形式のJSONを固定ファイルで持ち、毎回読めることを確認
- `npm run dev` で幅375px・**端末の文字サイズを最大**にして、横スクロールが出ず全ボタンが押せる
- 入力 → ブラウザを閉じる → 再度開いてデータが残っている
- **機内モードで起動して閲覧できる**（Lighthouseではなく実機で。PWAカテゴリは廃止済み）
- **音声入力**: 実機のiPhoneとAndroidの両方で、30秒以上話し続けても止まらず、結果を手で直せる
- **写真**: iPhoneで撮った縦写真が**横倒しにならない**。10枚添付しても動作が重くならない
- 控えをLINEで自分に送り、**別端末で開いて読める**こと、印刷して1枚に収まること
